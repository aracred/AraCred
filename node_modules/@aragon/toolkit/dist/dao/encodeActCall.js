"use strict";

Object.defineProperty(exports, "__esModule", {
  value: true
});
exports.default = void 0;

var _web3EthAbi = _interopRequireDefault(require("web3-eth-abi"));

function _interopRequireDefault(obj) { return obj && obj.__esModule ? obj : { default: obj }; }

/**
 * Encode ACT function call
 * @param {string} signature Function signature
 * @param {any[]} params
 */
var _default = (signature, params = []) => {
  const sigBytes = _web3EthAbi.default.encodeFunctionSignature(signature);

  const types = signature.replace(')', '').split('(')[1]; // No params, return signature directly

  if (types === '') {
    return sigBytes;
  }

  const paramBytes = _web3EthAbi.default.encodeParameters(types.split(','), params);

  return `${sigBytes}${paramBytes.slice(2)}`;
};

exports.default = _default;
//# sourceMappingURL=encodeActCall.js.map